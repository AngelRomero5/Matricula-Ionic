/*
  Las paginas que se van a incorporar en la aplicación son: 
  - Selección de curso (pantalla donde salen los cursos en forma de tarjeta con su informacion -> El estudiante selecciona un curso
    -> Selecciona boton de matricularse (va a haber un boton para cerrar la tarjeta) -> Le aparece al estudiante una pantalla con un 
    form para entrar sus datos y matricularse -> 
  
  - Creación de curso ( en esta pantalla el usuario municipal entra los datos del curso y presiona el boton "guardar")

  - Creación de usuario ( aquí el usuario selecciona si es empleado, estudiante(o tutor), maestro (rol) y luego entra su informacion personal)

  - Ajustes de usuario (pantalla que permite modificar los datos del usuario)

  - Cursos matriculados (aqui aparecen los cursos que los estudiantes tienen matriculados, )
*/